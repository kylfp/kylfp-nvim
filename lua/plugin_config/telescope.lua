require('telescope').setup {
    pickers = {
        find_files = {
            previewer = false,
            theme = 'dropdown',
        },
        buffers = {
            previewer = false,
            theme = 'dropdown',
        }
    }
}
