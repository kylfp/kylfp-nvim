require('onedark').setup {
  style = 'darker',
  transparent = false,

  code_style = {
    comments = 'italic',
    keywords = 'italic',
    functions = 'italic',
    strings = 'none',
    variables = 'bold',
  }
}
require('onedark').load()
